# 复制为 deploy.env 并填入你的服务器信息。
# deploy.env 已被 .gitignore 忽略，不会进仓库（避免泄露 IP / 路径）。
SSH_TARGET=user@your-server-ip       # ssh 用户@主机，例如 deploy@example.com
REMOTE_DIR=/www/wwwroot/your-site    # 服务器上网站根目录
